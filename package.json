{
  "name": "mubly-site-ai-beta",
  "version": "1.0.0",
  "private": true,
  "description": "Beta app for Mubly using Vercel Serverless Functions",
  "type": "module", 
  "scripts": {
    "start": "echo 'Frontend is static' && exit 0"
  },
  "engines": {
    "node": ">=18.x"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  }
}
